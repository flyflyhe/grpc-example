syntax = "proto3";
package test;

service TestService {
    rpc Search(TestRequest) returns (TestResponse){};
};

message TestRequest {
    string name = 1;
}

message TestResponse {
    string name = 1;
}